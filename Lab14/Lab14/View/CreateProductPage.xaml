<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:ViewModel="clr-namespace:Lab14.ViewModel"
             x:Class="Lab14.View.CreateProductPage">
    <ContentPage.BindingContext>
        <ViewModel:ProductViewModel />
    </ContentPage.BindingContext>
    <ContentPage.Content>
        <StackLayout>
            <StackLayout.Padding>
                <OnPlatform 
                    x:TypeArguments="Thickness"
                    Android="20"
                    iOS="50"/>
            </StackLayout.Padding>
            <Label Text="Producto :" />
            <Label Text="ID Producto" />
            <Entry Placeholder="ID"
                   Text="{Binding IdProd}"/>
            <Label Text="Nombre" />
            <Entry Placeholder="Nombre"
                   Text="{Binding Nombre}"/>
            <Label Text="Fecha Reestablecimiento producto" />
            <DatePicker Date="{Binding Fecha}"/>
            <Label Text="Stock" />
            <Entry Placeholder="Stock"
                   Text="{Binding Stock}"
                   Keyboard="Numeric">
                <Entry.Behaviors>
                    <ViewModel:NumericValidationBehavior/>
                </Entry.Behaviors>
            </Entry>
            <Picker Title="¿Disponible para delivery?"
                    SelectedIndex="{Binding Delivery}">
                <Picker.ItemsSource>
                    <x:Array Type="{x:Type x:String}">
                        <x:String>No Disponible</x:String>
                        <x:String>Disponible</x:String>
                    </x:Array>
                </Picker.ItemsSource>
            </Picker>
            <Button Text="Guardar"
                    Command="{Binding CreateProduct}"/>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>